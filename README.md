# template mini app telegram

**Install Dependencies**:

    ```bash
    npm install
    ```

**Run the Development Server**:

    ```bash
    npm run dev
    ```

**Open Your Browser**:

    Navigate to localhost to see the initial setup.

